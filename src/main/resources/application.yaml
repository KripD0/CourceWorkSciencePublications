spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/scientificsystem
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
springdoc:
  swagger-ui:
    enabled: true
    path: /swag